# &#x20;原型标注规范

1\. 每个可交互元素（按钮、输入框、图表）添加 \`data-marker\` 属性，说明业务逻辑。

&#x20;  示例：\`\<Button data-marker="导出当前筛选条件下的销售明细 Excel">导出\</Button>\`

2\. 页面底部添加 \`\<details>\` 折叠面板「交互说明」，汇总所有标注。

3\. 表格列头添加 \`title\` 属性，说明字段含义及计算口径。

【标注要求】
1. 为所有按钮、输入框、图表容器添加 data-marker 属性，属性值为业务说明（例如：导出按钮的 data-marker="导出当前筛选的销售数据 Excel"）。
2. 在页面底部（footer 区域）添加一个 <details> 折叠面板，默认折叠，标题为“📌 交互说明”，内部以列表形式汇总页面所有 data-marker 及其含义。
3. 表格的每一列 <th> 添加 title 属性，说明该列数据的计算口径或来源。


请在 App.vue 中添加以下功能：

1. 在 mounted 生命周期中，动态创建一个浮动面板（固定定位，右下角，宽度 320px，白色背景，有边框和阴影，圆角 8px，内边距 12px）。
2. 面板默认显示：“📌 标注说明\n点击任意带标注的元素，查看需求详情。”
3. 为页面中所有带有 data-marker 属性的元素绑定点击事件：点击后，将浮动面板的内容更新为“📌 标注说明\n[该元素的 data-marker 值]”。
4. 确保新增的代码不影响现有样式和布局。

生成完整的项目代码，包括 App.vue、主页面组件（可选多个）、package.json 等。请逐步生成，每一步完成后等待我确认。